{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "compile: user",
			"type": "shell",
			"command": "cargo build -p user"
		},
		{
			"label": "compile: health",
			"type": "shell",
			"command": "cargo build -p health"
		}
	]
}
