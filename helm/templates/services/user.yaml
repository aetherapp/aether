apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-user
  labels:
    app: {{ .Release.Name }}-user
spec:
  replicas: 3
  selector:
    matchLabels:
      app: {{ .Release.Name }}-user
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-user
    spec:
      containers:
        - name: {{ .Release.Name }}-user
          image: aethercloud/user:{{ .Values.release }}
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "32Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5

---

apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-user
spec:
  selector:
    app: {{ .Release.Name }}-user
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
